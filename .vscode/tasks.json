{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "process",     // <- change from "dotnet" to "process"
            "command": "dotnet",
            "args": ["build"],
            "problemMatcher": "$msCompile",
            "group": "build"
        }
    ]
}
